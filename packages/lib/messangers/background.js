export default {
  init(duplex) {
    this.duplex = duplex
  }

  /*func(data) {
    this.duplex.send('popup', 'func', data, false)
  },

  func2(data) {
    this.duplex.send(
      'tab',
      'tunnel',
      {
        action: 'func2',
        data:
      },
      false
    )
  },*/
}
